<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >


    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Include partial results"
        android:id="@+id/partialSelections"
        android:checked="false"
        android:layout_gravity="left"
        android:onClick="onCheckboxClicked"
        android:layout_marginLeft="0dp"
        android:layout_alignTop="@+id/findSelected"
        android:layout_toStartOf="@+id/findSelected"
        android:layout_marginEnd="45dp"
        android:layout_marginTop="10dp" />

    <ListView android:id="@+id/listView1" android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="100dp" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="Generate Cocktails"
        android:id="@+id/findSelected"
        android:layout_marginBottom="35dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="30dp"
        android:textColor="#FFFFFF"
        android:background="@color/accent" />

</RelativeLayout>